import ResourcePage from '../common/ResourcePage.jsx';
import { resourceConfigs } from '../../data/resourceConfig.js';

export default function AnneesAcademiquesPage() {
  return <ResourcePage config={resourceConfigs.anneesAcademiques} />;
}
